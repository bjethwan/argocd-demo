# Default values for cluster-charts.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

suffix: k8s2
cluster: https://api.k8s2.ap-south-1.bjethwan.xyz
project: default

